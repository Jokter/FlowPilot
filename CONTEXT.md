# 需求交付流程平台

本上下文描述团队如何通过固定研发方法、人工确认与 Codex 协作，将一条需求推进到可评审的 GitHub 草稿 PR。

## 流程语言

**需求（Requirement）**：
一次需要被分析、设计、实现和交付的团队工作请求；第一版只关联一个主代码仓库。
_避免：工单、任务、项目_

**流程模板（Workflow Template）**：
团队对某类需求规定的标准交付方法，是多个已发布流程版本的逻辑身份。
_避免：流程实例、工作流运行_

**流程版本（Workflow Version）**：
流程模板一次不可变的已发布定义，固定阶段、节点、转换、完成门禁和 Skill Suite 版本。
_避免：最新流程、可编辑流程_

**流程运行（Workflow Run）**：
一条需求按照某个固定流程版本推进的实际过程。
_避免：流程模板、Codex 会话_

**阶段（Stage）**：
面向员工展示的一组相关流程节点，用于表达需求当前处于研发生命周期的哪个部分。
_避免：节点、状态_

**节点定义（Node Definition）**：
流程版本中一个必须完成的工作步骤，规定参与者、输入、产物和完成门禁。
_避免：节点运行、Skill_

**节点运行（Node Run）**：
某次流程运行中节点定义的一次实际执行与结果记录。
_避免：节点定义、Thread_

**完成门禁（Completion Gate）**：
节点或阶段进入后续步骤前必须满足的、可验证的产物与人工确认条件。
_避免：流程依赖、Skill 依赖_

## AI 协作语言

**Skill**：
以 `SKILL.md` 为入口、指导 Codex 完成一类工作的可复用方法包，可附带参考资料、模板、脚本和资源。
_避免：节点、工具、提示词_

**Skill Suite**：
团队批准并作为一个整体发布的方法论套件；一个流程版本固定使用一个套件版本。
_避免：Skill Chain、任意 Skill 集合_

**Skill Chain**：
节点定义中按固定顺序执行的多个 Skill；所有 Skill 必须来自同一 Skill Suite 版本。
_避免：Skill Suite、动态 Agent 规划_

**Codex Thread**：
一次可持续多轮、恢复、分叉和压缩的 Codex 协作上下文，只能在同一需求内复用。
_避免：流程运行、节点运行、聊天消息_

**Turn**：
Codex Thread 中由一次用户输入或一次系统 Skill 调用触发的执行轮次。
_避免：节点、流程步骤_

## 交付语言

**产物（Artifact）**：
节点运行产生或使用的版本化工作成果，例如需求分析、SDD、计划、代码变更或测试报告。
_避免：聊天内容、运行日志_

**证据（Evidence）**：
用于证明完成门禁已满足的可追溯事实或引用，例如审批记录、测试结果、提交摘要或 GitHub 检查状态。
_避免：产物、普通日志_

**交付（Delivery）**：
第一版中，需求的隔离分支已推送并成功创建可供团队继续评审的 GitHub 草稿 PR。
_避免：合并、生产发布、部署_
